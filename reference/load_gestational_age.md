# Load the example gestational age datasets

Loads example datasets included with the package into the user's
environment.

## Usage

``` r
load_gestational_age()
```

## Value

Invisibly returns the names of the objects loaded.
