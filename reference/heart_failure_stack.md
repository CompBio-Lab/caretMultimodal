# Pre-trained `caret_stack` on Heart Failure Datasets

A `caret_stack` object pre-trained on `heart_failure_datasets`. Used in
examples throughout the caretMultimodal package.

## Usage

``` r
heart_failure_stack
```

## Format

An object of class `caret_stack` of length 2.

## See also

[`heart_failure_datasets`](https://compbio-lab.github.io/caretMultimodal/reference/heart_failure_datasets.md)
